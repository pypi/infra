apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: view-serviceaccounts
  namespace: vault
rules:
- apiGroups:
  - ""
  resources:
  - serviceaccounts
  verbs:
  - get
  - list
  - watch
